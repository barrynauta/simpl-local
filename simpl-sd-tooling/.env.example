# Host ports (all chosen to be clash-free next to the other simpl-local stacks)
UI_PORT=4324
SDTOOLING_API_PORT=8087
VALIDATION_API_PORT=8088
STUBS_PORT=8089

# Optional code.europa.eu Personal Access Token (read_api scope).
# Only needed if anonymous resolution of eu.europa.ec.simpl Maven artifacts
# or @simpl/vue-components npm packages fails during image builds.
GITLAB_TOKEN=
