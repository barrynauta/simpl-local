repos/
build/
.env
discovery-reports/
plans/
*.log
target/
# samples/ ttl files are copied from repos/ at start.sh time; only the README is committed
samples/*.ttl
# mailpit-relay.yaml is generated by start.sh from .env values when
# --with-notifications is used; contains relay credentials, never commit
mailpit-relay.yaml
